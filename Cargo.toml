[workspace]
resolver = "2"
members = [
    "rust/crates/pvm-gpu-wire",
    "rust/crates/pvm-motion-wire",
    "rust/crates/pvm-runtime",
    "rust/crates/pvm-runtime-assets",
    "rust/crates/pvm-assets-export",
]

[workspace.package]
version = "0.1.9"
edition = "2021"
license = "MPL-2.0"
repository = "https://github.com/paritytech/pvm-host-runtime"

[workspace.dependencies]
uniffi = "0.32.0"

[workspace.lints.rust]
missing_docs = "warn"
unsafe_code = "deny"

[profile.release]
opt-level = "z"
lto = "fat"
codegen-units = 1
panic = "abort"
strip = "symbols"
debug = false
