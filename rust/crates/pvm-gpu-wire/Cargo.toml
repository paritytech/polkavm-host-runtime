[package]
name = "pvm-gpu-wire"
version = "0.1.14"
edition = "2021"
description = "Bounded host-neutral Tri2D and WebGPU wire protocols for PolkaVM applications"
license = "MPL-2.0"
repository = "https://github.com/paritytech/pvm-host-runtime"
publish = false

[features]
default = ["tri2d-validation"]
tri2d-validation = []

[lib]
crate-type = ["rlib"]
